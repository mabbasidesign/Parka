@model IEnumerable<Demo.Entities.Product>

@{
    ViewBag.Title = "List";
}

<h2>Product List</h2>
<h2></h2>

@foreach(var p in Model)
{
    <div>
        <h3>@p.Name</h3>
        <h4>@p.Price.ToString("c")</h4>
        @p.Description
        <hr />
    </div>
}


